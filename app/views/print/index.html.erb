<h1><span class="glyphicon glyphicon-print"></span> <%= t("printing") %></h1>

<h2><%= t("pendingtags") %></h2>

<p>
<%= link_to({ action: :flush }, { class: "btn btn-danger btn-small" }) do %>
    <span class="glyphicon glyphicon-print"></span> <%= t("forceprint") %>
<% end %>
</p>

<%= render partial: "people/list", locals: { people: @queued, with_church: true, no_sort: true } %>

<p><strong><%= t("total") %>:</strong> <%= @queued.size %></p>

<h2><%= t("justprinted") %></h2>

<%= render partial: "people/list", locals: { people: @printed, with_church: true, no_sort: true } %>